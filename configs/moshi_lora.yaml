# Track A — Moshi LoRA fine-tune config.
# Generated by `duplex-bol moshi init-config`; safe to hand-edit (it round-trips
# through MoshiFinetuneConfig.from_yaml). Check it before renting a GPU:
#   duplex-bol moshi vram --config configs/moshi_lora.yaml
base_model: kyutai/moshiko-pytorch-bf16
tokenizer_path: data/trackA/urdu_tokenizer/urdu.model
data_index: data/trackA/index.jsonl
duration_sec: 100.0
batch_size: 16
max_steps: 2000
learning_rate: 2.0e-05
gradient_checkpointing: true
seed: 0
lora:
  rank: 128
  scaling: 2.0
  ff: true
